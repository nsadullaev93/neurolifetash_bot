-- Скидка 10% при оплате более 20 занятий в месяц (по усмотрению
-- администрации центра — не авто-правило). Хранится на конкретной оплате:
-- rateSnapshot этой оплаты уже содержит эффективную (возможно, сниженную)
-- ставку, поэтому формула сверки (P - billable) * S работает без изменений.
ALTER TABLE "MonthlyPayment" ADD COLUMN "discountApplied" BOOLEAN NOT NULL DEFAULT false;
